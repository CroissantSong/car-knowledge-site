---
layout: page
title: 页面未找到
---

<script setup>
import { useRouter } from 'vitepress'
import { ref, onMounted } from 'vue'

const router = useRouter()
const searchQuery = ref('')
const searchInput = ref(null)

onMounted(() => {
  if (searchInput.value) {
    searchInput.value.focus()
  }
})

function goHome() {
  router.go('/car-knowledge-site/')
}

function handleSearch() {
  if (!searchQuery.value.trim()) return
  router.go('/car-knowledge-site/?search=' + encodeURIComponent(searchQuery.value.trim()))
}

function handleKeydown(e) {
  if (e.key === 'Enter') {
    handleSearch()
  }
}
</script>

<div class="not-found-page">
  <div class="not-found-content">
    <div class="not-found-illustration">
      <div class="not-found-emoji">🚗❓</div>
    </div>

    <h1 class="not-found-title">页面未找到</h1>
    <p class="not-found-desc">
      看起来这条路还没修好。试试搜索关键词，或者回到知识地图找找方向。
    </p>

    <div class="not-found-search">
      <input
        ref="searchInput"
        v-model="searchQuery"
        type="text"
        placeholder="搜索知识点..."
        class="not-found-search-input"
        @keydown="handleKeydown"
      />
      <button class="not-found-search-btn" @click="handleSearch">
        搜索
      </button>
    </div>

    <div class="not-found-links">
      <a class="not-found-link primary" href="/car-knowledge-site/">
        🏠 回到首页
      </a>
      <a class="not-found-link" href="/car-knowledge-site/guide/">
        🚗 整车认知
      </a>
      <a class="not-found-link" href="/car-knowledge-site/path">
        🗺️ 30天地图
      </a>
      <a class="not-found-link" href="/car-knowledge-site/glossary">
        📖 术语表
      </a>
      <a class="not-found-link" href="/car-knowledge-site/quiz/">
        📝 题库
      </a>
      <a class="not-found-link" href="/car-knowledge-site/roles-guide/">
        🧭 岗位导航
      </a>
    </div>
  </div>
</div>

<style scoped>
.not-found-page {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: calc(100vh - 200px);
  padding: 40px 20px;
}

.not-found-content {
  max-width: 560px;
  text-align: center;
}

.not-found-illustration {
  margin-bottom: 28px;
}

.not-found-emoji {
  font-size: 64px;
  line-height: 1;
  animation: notFoundBounce 2s ease-in-out infinite;
}

@keyframes notFoundBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8px); }
}

.not-found-title {
  margin: 0 0 12px;
  font-size: 32px;
  font-weight: 850;
  color: var(--auto-ink, #14213d);
  line-height: 1.2;
  border: 0;
  padding: 0;
}

.not-found-desc {
  margin: 0 0 28px;
  font-size: 16px;
  color: var(--vp-c-text-2, #4a5a70);
  line-height: 1.7;
}

/* Search */
.not-found-search {
  display: flex;
  gap: 8px;
  margin-bottom: 28px;
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
}

.not-found-search-input {
  flex: 1;
  padding: 12px 16px;
  border: 2px solid var(--auto-border, rgba(20, 33, 61, 0.12));
  border-radius: 10px;
  background: var(--auto-card-strong, #ffffff);
  color: var(--vp-c-text-1, #162033);
  font-size: 15px;
  font-weight: 500;
  transition: border-color 0.2s, box-shadow 0.2s;
  outline: none;
}

.not-found-search-input:focus {
  border-color: var(--vp-c-brand-2, #0d9488);
  box-shadow: 0 0 0 3px var(--vp-c-brand-soft, rgba(20, 184, 166, 0.14));
}

.not-found-search-input::placeholder {
  color: var(--vp-c-text-3, #6c7a8d);
}

.not-found-search-btn {
  padding: 12px 24px;
  border: none;
  border-radius: 10px;
  background: linear-gradient(135deg, var(--vp-c-brand-1, #0f766e), var(--auto-blue, #2563eb));
  color: white;
  font-size: 15px;
  font-weight: 700;
  cursor: pointer;
  transition: transform 0.15s, opacity 0.15s;
  white-space: nowrap;
}

.not-found-search-btn:hover {
  opacity: 0.9;
  transform: translateY(-1px);
}

.not-found-search-btn:active {
  transform: scale(0.97);
}

/* Links */
.not-found-links {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  justify-content: center;
}

.not-found-link {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 10px 18px;
  border: 1px solid var(--auto-border, rgba(20, 33, 61, 0.12));
  border-radius: 10px;
  background: var(--auto-card-strong, #ffffff);
  color: var(--vp-c-text-1, #162033);
  font-size: 14px;
  font-weight: 650;
  text-decoration: none !important;
  transition: border-color 0.18s, color 0.18s, transform 0.18s, box-shadow 0.18s;
  min-height: 44px;
}

.not-found-link:hover {
  border-color: var(--vp-c-brand-2, #0d9488);
  color: var(--vp-c-brand-1, #0f766e);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(15, 23, 42, 0.08);
}

.not-found-link.primary {
  border-color: transparent;
  background: linear-gradient(135deg, var(--vp-c-brand-1, #0f766e), var(--auto-blue, #2563eb));
  color: white;
}

.not-found-link.primary:hover {
  color: white;
  opacity: 0.9;
}

/* Dark mode */
.dark .not-found-search-input {
  background: var(--auto-card-strong, #151e2c);
}

.dark .not-found-link {
  background: var(--auto-card-strong, #151e2c);
}

/* Mobile */
@media (max-width: 640px) {
  .not-found-page {
    min-height: calc(100vh - 160px);
    padding: 32px 16px;
  }

  .not-found-title {
    font-size: 26px;
  }

  .not-found-desc {
    font-size: 15px;
  }

  .not-found-search {
    flex-direction: column;
  }

  .not-found-search-input,
  .not-found-search-btn {
    width: 100%;
    min-height: 48px;
  }

  .not-found-links {
    flex-direction: column;
    align-items: stretch;
  }

  .not-found-link {
    justify-content: center;
  }
}
</style>
